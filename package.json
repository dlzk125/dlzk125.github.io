{
  "name": "james.github.io",
  "version": "1.0.0",
  "private": true,
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "3000"
    }
  },
  "scripts": {
    "dev": "nuxt",
    "prod": "npm run build && npm run start",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "publish": "npm run generate && npm run deploy",
    "browserslist": "browserslist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dlzk125/dlzk125.github.io.git"
  },
  "browserslist": [
    "defaults",
    "ie 10-11",
    "last 3 firefox version",
    "last 20 chrome version",
    "Samsung > 4",
    "android > 4",
    "ios >= 9"
  ],
  "dependencies": {
    "@nuxt/content": "^1.9.0",
    "@nuxtjs/axios": "^5.12.2",
    "@nuxtjs/style-resources": "^1.0.0",
    "core-js": "^3.6.5",
    "directory-tree": "^2.2.4",
    "node-sass": "^4.14.1",
    "nuxt": "^2.11.0",
    "nuxt-polyfill": "^1.0.3",
    "sass-loader": "^8.0.2"
  },
  "devDependencies": {
    "@nuxtjs/svg": "^0.1.12",
    "push-dir": "^0.4.1"
  }
}
